{
  "name": "bping",
  "version": "1.0.22",
  "description": "Basecamp Pings — native wrapper that embeds the real Basecamp chat",
  "main": "main.js",
  "scripts": {
    "start": "electron .",
    "package": "electron-packager . bping --platform=darwin --arch=arm64 --overwrite --out=dist --icon=icon.icns --app-bundle-id=com.decent.bping --app-category-type=public.app-category.productivity",
    "dmg": "./build-dmg.sh",
    "make-ico": "node tools/make-ico.js",
    "package:win": "node tools/make-ico.js && electron-packager . bping --platform=win32 --arch=x64 --overwrite --out=dist --icon=icon.ico --win32metadata.CompanyName=\"Decent Espresso\" --win32metadata.ProductName=bping --win32metadata.FileDescription=\"Basecamp Pings client\""
  },
  "devDependencies": {
    "electron": "^42.3.0",
    "@electron/packager": "^18.4.0"
  }
}
